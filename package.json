{
  "name": "agent-protocol-validator",
  "description": "Enforces strict schema validation and security policy compliance for inter-agent communication messages",
  "version": "1.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "agent-protocol-validator": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "build": "tsc",
    "start": "bun run src/cli.ts"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
